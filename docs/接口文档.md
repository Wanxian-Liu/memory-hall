# Mimir-Core 接口文档

> 生成时间: 2026-04-11
> 模块数量: 111
> 描述: 记忆殿堂v2.0 DAME 核心接口定义

---

## 目录

1. [agent — 代理系统](#1-agent--代理系统)
2. [audit — 审计日志](#2-audit--审计日志)
3. [base_wal — WAL协议](#3-base_wal--wal协议)
4. [classifier — 分类引擎](#4-classifier--分类引擎)
5. [cli — 命令行接口](#5-cli--命令行接口)
6. [config — 配置加载](#6-config--配置加载)
7. [evolve — 自进化引擎](#7-evolve--自进化引擎)
8. [extractor — 内容提取](#8-extractor--内容提取)
9. [fence — 围栏隔离](#9-fence--围栏隔离)
10. [gateway — 网关](#10-gateway--网关)
11. [health — 健康检查](#11-health--健康检查)
12. [integrate — 集成系统](#12-integrate--集成系统)
13. [interfaces — 接口抽象](#13-interfaces--接口抽象)
14. [introspection — 自省模块](#14-introspection--自省模块)
15. [memory_layer — 记忆层级](#15-memory_layer--记忆层级)
16. [mini_agent — 轻量代理](#16-mini_agent--轻量代理)
17. [normalizer — 归一化](#17-normalizer--归一化)
18. [optimize — 优化器](#18-optimize--优化器)
19. [permission — 权限引擎](#19-permission--权限引擎)
20. [pipeline — 流水线](#20-pipeline--流水线)
21. [plugin — 插件系统](#21-plugin--插件系统)
22. [repair — 备份修复](#22-repair--备份修复)
23. [sensory — 感知引擎](#23-sensory--感知引擎)
24. [task — 任务管理](#24-task--任务管理)
25. [utils — 工具函数](#25-utils--工具函数)

---

## 1. agent — 代理系统

### 1.1 数据模型 (`agent/models.py`)

#### 类: `RoleType` (Enum)
```python
class RoleType(Enum):
    RESEARCHER = "researcher"   # 研究员 - 深度调研
    DEVELOPER = "developer"     # 开发者 - 代码实现
    VALIDATOR = "validator"    # 验证者 - 测试审查
    RECORDER = "recorder"      # 记录员 - 文档记录
    COORDINATOR = "coordinator" # 协调者 - 任务协调
```

#### 类: `AgentState` (Enum)
```python
class AgentState(Enum):
    CREATED = "created"    # 已创建
    RUNNING = "running"     # 运行中
    IDLE = "idle"           # 空闲等待
    COMPLETED = "completed"  # 已完成
    FAILED = "failed"       # 失败
    STOPPED = "stopped"     # 已停止
```

#### 类: `TaskStatus` (Enum)
```python
class TaskStatus(Enum):
    PENDING = "pending"    # 待处理
    RUNNING = "running"    # 执行中
    COMPLETED = "completed" # 已完成
    FAILED = "failed"     # 失败
    CANCELLED = "cancelled" # 已取消
```

#### 类: `Role`
```python
@dataclass
class Role:
    name: str
    role_type: RoleType
    description: str = ""
    capabilities: List[str] = field(default_factory=list)
    max_concurrent: int = 3
    timeout_seconds: int = 300
    metadata: Dict[str, Any] = field(default_factory=dict)
    
    def can_handle(self, task_type: str) -> bool: ...
```

#### 类: `Agent`
```python
@dataclass
class Agent:
    agent_id: str
    role: Role
    state: AgentState = AgentState.CREATED
    current_task_id: Optional[str] = None
    heartbeat_count: int = 0
    last_heartbeat: Optional[datetime] = None
    created_at: datetime = field(default_factory=datetime.now)
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    context: Dict[str, Any] = field(default_factory=dict)
    result: Optional[Any] = None
    error: Optional[str] = None
    
    def is_alive(self) -> bool: ...
    def mark_heartbeat(self) -> None: ...
```

#### 类: `Task`
```python
@dataclass
class Task:
    task_id: str
    task_type: str           # 任务类型，匹配role.capabilities
    description: str
    priority: int = 0        # 优先级，数字越大优先级越高
    required_role: Optional[RoleType] = None
    payload: Dict[str, Any] = field(default_factory=dict)
    status: TaskStatus = TaskStatus.PENDING
    assigned_agent_id: Optional[str] = None
    created_at: datetime = field(default_factory=datetime.now)
    started_at: Optional[datetime] = None
    completed_at: Optional[datetime] = None
    result: Optional[Any] = None
    error: Optional[str] = None
    retry_count: int = 0
    max_retries: int = 3
    
    def __lt__(self, other: 'Task') -> bool: ...  # 支持heapq比较
    def can_retry(self) -> bool: ...
```

#### 类: `TaskResult`
```python
@dataclass
class TaskResult:
    task_id: str
    agent_id: str
    success: bool
    result: Optional[Any] = None
    error: Optional[str] = None
    execution_time_ms: int = 0
    timestamp: datetime = field(default_factory=datetime.now)
```

---

### 1.2 角色注册表 (`agent/role_registry.py`)

#### 类: `RegistryInner`
```python
@dataclass
class RegistryInner:
    roles: Dict[str, Role] = field(default_factory=dict)
    roles_by_type: Dict[RoleType, List[str]] = field(default_factory=dict)
    counter: int = 0  # 全局递增ID
    
    def next_id(self) -> int: ...
```

#### 类: `RoleRegistry`
```python
class RoleRegistry:
    def __init__(self): ...
    
    # ---- 公共API ----
    def register(self, role: Role) -> bool: ...
    def unregister(self, name: str) -> bool: ...
    def get(self, name: str) -> Optional[Role]: ...
    def get_by_type(self, role_type: RoleType) -> List[Role]: ...
    def find_can_handle(self, task_type: str) -> List[Role]: ...
    def list_all(self) -> List[Role]: ...
    def list_types(self) -> Set[RoleType]: ...
    def count(self) -> int: ...
    def clear(self) -> None: ...
```

#### 全局函数
```python
def get_global_registry() -> RoleRegistry: ...
```

---

### 1.3 生命周期管理器 (`agent/lifecycle_manager.py`)

#### 类: `LifecycleConfig`
```python
@dataclass
class LifecycleConfig:
    heartbeat_interval_seconds: int = 30      # 心跳间隔
    heartbeat_timeout_seconds: int = 120      # 心跳超时
    max_idle_seconds: int = 300               # 最大空闲时间
    cleanup_interval_seconds: int = 60         # 清理检查间隔
```

#### 类: `AgentLifecycleManager`
```python
class AgentLifecycleManager:
    def __init__(
        self,
        registry: Optional[RoleRegistry] = None,
        config: Optional[LifecycleConfig] = None
    ): ...
    
    # ---- 公共API ----
    def set_callbacks(
        self,
        on_spawn: Optional[Callable[[Agent], None]] = None,
        on_terminate: Optional[Callable[[Agent, str], None]] = None,
        on_heartbeat: Optional[Callable[[Agent], None]] = None,
        on_state_change: Optional[Callable[[Agent, AgentState, AgentState], None]] = None,
    ) -> None: ...
    
    def spawn(
        self,
        role_name: str,
        agent_id: Optional[str] = None,
        context: Optional[Dict[str, Any]] = None
    ) -> Optional[Agent]: ...
    
    def spawn_by_type(
        self,
        role_type: RoleType,
        agent_id: Optional[str] = None,
        context: Optional[Dict[str, Any]] = None
    ) -> Optional[Agent]: ...
    
    def heartbeat(self, agent_id: str) -> bool: ...
    def terminate(self, agent_id: str, reason: str = "normal", result: Optional[Any] = None) -> bool: ...
    def set_idle(self, agent_id: str) -> bool: ...
    def assign_task(self, agent_id: str, task_id: str) -> bool: ...
    def get(self, agent_id: str) -> Optional[Agent]: ...
    def list_all(self) -> List[Agent]: ...
    def list_by_state(self, state: AgentState) -> List[Agent]: ...
    def list_alive(self) -> List[Agent]: ...
    def count(self) -> int: ...
    def count_alive(self) -> int: ...
    def start_cleanup_thread(self) -> None: ...
    def stop_cleanup_thread(self) -> None: ...
```

#### 全局函数
```python
def get_global_lifecycle_manager() -> AgentLifecycleManager: ...
```

---

### 1.4 任务分发器 (`agent/task_dispatcher.py`)

#### 类: `DispatcherConfig`
```python
@dataclass
class DispatcherConfig:
    max_pending_tasks: int = 1000       # 最大待处理任务数
    default_priority: int = 0            # 默认优先级
    assign_timeout_seconds: int = 60     # 任务分配超时
    retry_delay_seconds: int = 5         # 重试延迟
```

#### 类: `TaskDispatcher`
```python
class TaskDispatcher:
    def __init__(
        self,
        registry: Optional[RoleRegistry] = None,
        lifecycle_manager: Optional[AgentLifecycleManager] = None,
        config: Optional[DispatcherConfig] = None
    ): ...
    
    # ---- 公共API ----
    def set_callbacks(
        self,
        on_task_assigned: Optional[Callable[[Task, str], None]] = None,
        on_task_completed: Optional[Callable[[Task, TaskResult], None]] = None,
        on_task_failed: Optional[Callable[[Task, str], None]] = None,
    ) -> None: ...
    
    def submit(
        self,
        task_type: str,
        description: str,
        priority: int = 0,
        required_role: Optional[RoleType] = None,
        payload: Optional[Dict[str, Any]] = None,
        task_id: Optional[str] = None
    ) -> Optional[str]: ...
    
    def dispatch(self, task_id: str) -> bool: ...
    def complete(self, task_id: str, result: Any = None, error: Optional[str] = None) -> bool: ...
    def cancel(self, task_id: str) -> bool: ...
    def get(self, task_id: str) -> Optional[Task]: ...
    def list_all(self) -> List[Task]: ...
    def list_pending(self) -> List[Task]: ...
    def list_running(self) -> List[Task]: ...
    def list_completed(self) -> List[Task]: ...
    def count(self) -> int: ...
    def count_pending(self) -> int: ...
    def start_dispatch_thread(self) -> None: ...
    def stop_dispatch_thread(self) -> None: ...
```

#### 全局函数
```python
def get_global_dispatcher() -> TaskDispatcher: ...
```

---

## 2. audit — 审计日志

### 2.1 核心 (`audit/audit.py`)

#### 枚举: `AuditLevel` (IntEnum)
```python
class AuditLevel(IntEnum):
    DEBUG = 10
    INFO = 20
    WARNING = 30
    ERROR = 40
    CRITICAL = 50
```

#### 枚举: `AuditCategory` (IntEnum)
```python
class AuditCategory(IntEnum):
    READ = 1          # 读取操作
    WRITE = 2         # 写入操作
    DELETE = 3        # 删除操作
    PERMISSION = 4   # 权限变更
    SECURITY = 5      # 安全相关
    SYSTEM = 6        # 系统操作
    QUERY = 7         # 查询操作
    EXPORT = 8        # 导出操作
    IMPORT = 9        # 导入操作
    CONFIG = 10       # 配置变更
```

#### 枚举: `RiskLevel` (IntEnum)
```python
class RiskLevel(IntEnum):
    LOW = 1           # 低风险
    MEDIUM = 2        # 中风险
    HIGH = 3          # 高风险
    CRITICAL = 4      # 极高风险
```

#### 类: `AuditEntry`
```python
class AuditEntry:
    def __init__(
        self,
        operation: str,
        category: Union[AuditCategory, int],
        level: Union[AuditLevel, int] = AuditLevel.INFO,
        risk: Union[RiskLevel, int] = RiskLevel.LOW,
        actor: str = "system",
        target: str = "",
        details: Optional[Dict[str, Any]] = None,
        session_id: str = "",
        channel: str = "",
        success: bool = True,
        error: str = "",
        stack_trace: str = "",
        tags: Optional[List[str]] = None,
        duration_ms: int = 0,
    ): ...
    
    def to_dict(self) -> Dict[str, Any]: ...
    @classmethod
    def from_dict(cls, d: Dict[str, Any]) -> "AuditEntry": ...
```

#### 类: `_AuditDB`
```python
class _AuditDB:  # SQLite单例
    def insert(self, entry: AuditEntry): ...
    
    def query(
        self,
        start_time: Optional[datetime] = None,
        end_time: Optional[datetime] = None,
        operation: Optional[str] = None,
        category: Optional[AuditCategory] = None,
        level: Optional[AuditLevel] = None,
        risk: Optional[RiskLevel] = None,
        actor: Optional[str] = None,
        target: Optional[str] = None,
        session_id: Optional[str] = None,
        success: Optional[bool] = None,
        high_risk_only: bool = False,
        limit: int = 1000,
        offset: int = 0,
    ) -> List[AuditEntry]: ...
    
    def count(
        self,
        start_time: Optional[datetime] = None,
        end_time: Optional[datetime] = None,
        high_risk_only: bool = False,
    ) -> int: ...
    
    def get_high_risk_summary(self, days: int = 7) -> Dict[str, Any]: ...
    def get_recent_failures(self, hours: int = 24, limit: int = 100) -> List[AuditEntry]: ...
```

#### 类: `AuditLogger`
```python
class AuditLogger:
    def __init__(
        self,
        actor: str = "system",
        session_id: str = "",
        channel: str = "",
        write_text_log: bool = True,
        write_db: bool = True,
    ): ...
    
    # ---- 上下文管理器 ----
    def track(self, operation: str, target: str = "", category: Union[AuditCategory, int] = AuditCategory.SYSTEM, **kwargs) -> "AuditLogger": ...
    def __enter__(self) -> "AuditLogger": ...
    def __exit__(self, exc_type, exc_val, exc_tb): ...
    
    # ---- 直接记录 ----
    def log(
        self,
        operation: str,
        category: Union[AuditCategory, int] = AuditCategory.SYSTEM,
        level: Union[AuditLevel, int] = AuditLevel.INFO,
        risk: Optional[Union[RiskLevel, int]] = None,
        target: str = "",
        details: Optional[Dict[str, Any]] = None,
        success: bool = True,
        error: str = "",
        tags: Optional[List[str]] = None,
        duration_ms: int = 0,
    ): ...
    
    # ---- 快捷方法 ----
    def log_read(self, target: str, details: Optional[Dict[str, Any]] = None): ...
    def log_write(self, target: str, details: Optional[Dict[str, Any]] = None): ...
    def log_delete(self, target: str, details: Optional[Dict[str, Any]] = None): ...
    def log_security(self, operation: str, target: str = "", details: Optional[Dict[str, Any]] = None): ...
    def log_permission(self, action: str, target: str, details: Optional[Dict[str, Any]] = None): ...
    def log_error(self, operation: str, error: str, target: str = ""): ...
    
    # ---- 查询 ----
    def query(self, **kwargs) -> List[AuditEntry]: ...
    def query_high_risk(self, **kwargs) -> List[AuditEntry]: ...
    def count(self, **kwargs) -> int: ...
    def get_high_risk_summary(self, days: int = 7) -> Dict[str, Any]: ...
    def get_recent_failures(self, hours: int = 24) -> List[AuditEntry]: ...
```

#### 全局函数
```python
def get_audit_logger(actor: str = "system", session_id: str = "", channel: str = "") -> AuditLogger: ...
def log_operation(operation: str, category: Union[AuditCategory, int] = AuditCategory.SYSTEM, level: Union[AuditLevel, int] = AuditLevel.INFO, target: str = "", details: Optional[Dict[str, Any]] = None, actor: str = "system", session_id: str = "", channel: str = "", success: bool = True, error: str = "", tags: Optional[List[str]] = None) -> AuditEntry: ...
def log_high_risk_operation(operation: str, target: str = "", details: Optional[Dict[str, Any]] = None, actor: str = "system", session_id: str = "", channel: str = "", success: bool = True, error: str = "", tags: Optional[List[str]] = None) -> AuditEntry: ...
def query_logs(...) -> List[AuditEntry]: ...
def infer_risk(operation: str, category: AuditCategory) -> RiskLevel: ...
```

---

## 3. base_wal — WAL协议

### 3.1 核心 (`base_wal/wal.py`)

#### 枚举: `WALPhase`
```python
class WALPhase(Enum):
    PREPARE = "PREPARE"      # 准备阶段：记录操作意图
    EXECUTE = "EXECUTE"      # 执行阶段：记录执行结果
    COMMIT = "COMMIT"        # 提交阶段：确认完成
    ROLLBACK = "ROLLBACK"    # 回滚阶段：撤销操作
```

#### 枚举: `WALEntryType`
```python
class WALEntryType(Enum):
    TRANSACTION_BEGIN = "TRANSACTION_BEGIN"
    TRANSACTION_END = "TRANSACTION_END"
    WRITE = "WRITE"
    DELETE = "DELETE"
    COMMIT = "COMMIT"
    ROLLBACK = "ROLLBACK"
    CHECKPOINT = "CHECKPOINT"  # 检查点，用于压缩
```

#### 类: `WALEntry`
```python
@dataclass
class WALEntry:
    entry_id: str                    # 唯一标识
    entry_type: str                  # 条目类型
    transaction_id: str               # 事务ID
    phase: str                        # 当前阶段
    key: Optional[str] = None         # 操作键
    value: Optional[str] = None       # 操作值 (JSON序列化)
    timestamp: float = field(default_factory=time.time)
    prev_hash: str = ""              # 前一条目哈希
    checksum: str = ""               # 校验和
    
    def to_dict(self) -> Dict[str, Any]: ...
    @classmethod
    def from_dict(cls, d: Dict) -> 'WALEntry': ...
    def compute_checksum(self) -> str: ...
```

#### 类: `Transaction`
```python
@dataclass
class Transaction:
    transaction_id: str
    status: str                       # PREPARE, EXECUTE, COMMIT, ROLLBACK
    entries: List[WALEntry] = field(default_factory=list)
    created_at: float = field(default_factory=time.time)
    updated_at: float = field(default_factory=time.time)
```

#### 类: `WALManager`
```python
class WALManager:
    def __init__(
        self,
        wal_dir: Optional[str] = None,
        max_file_size: int = 64 * 1024 * 1024,  # 64MB
        max_files: int = 10,
    ): ...
    
    # ---- 公共API ----
    def begin(self) -> str: ...          # 开始事务，返回transaction_id
    def prepare_write(self, transaction_id: str, key: str, value: Any) -> bool: ...
    def execute_write(self, transaction_id: str, key: str, value: Any) -> bool: ...
    def commit(self, transaction_id: str) -> bool: ...
    def rollback(self, transaction_id: str) -> bool: ...
    def wal_write(self, key: str, value: Any) -> bool: ...
    def wal_delete(self, key: str) -> bool: ...
    def compact(self) -> int: ...         # 返回压缩的日志条数
    def recover(self) -> List[Dict[str, Any]]: ...
    def status(self) -> Dict[str, Any]: ...
```

#### 全局函数
```python
def get_default_wal_manager() -> WALManager: ...
def begin() -> str: ...
def prepare_write(transaction_id: str, key: str, value: Any) -> bool: ...
def execute_write(transaction_id: str, key: str, value: Any) -> bool: ...
def commit(transaction_id: str) -> bool: ...
def rollback(transaction_id: str) -> bool: ...
def wal_write(key: str, value: Any) -> bool: ...
def wal_delete(key: str) -> bool: ...
def compact() -> int: ...
def recover() -> List[Dict[str, Any]]: ...
def status() -> Dict[str, Any]: ...
```

---

## 4. classifier — 分类引擎

### 4.1 核心 (`classifier/classifier.py`)

#### 枚举: `TaskStatus`
```python
class TaskStatus(Enum):
    PENDING = "pending"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"
```

#### 类: `ClassificationResult`
```python
@dataclass
class ClassificationResult:
    category: str
    confidence: float
    reasoning: str = ""
    metadata: Dict[str, Any] = field(default_factory=dict)
```

#### 类: `ClassificationTask`
```python
@dataclass
class ClassificationTask:
    task_id: str
    content: str
    category: str = ""
    confidence: float = 0.0
    reasoning: str = ""
    status: TaskStatus = TaskStatus.PENDING
    created_at: float = field(default_factory=time.time)
    completed_at: Optional[float] = None
    metadata: Dict[str, Any] = field(default_factory=dict)
```

#### 类: `TaskRegistry`
```python
class TaskRegistry:
    def create(self, name: str, description: str = "", priority: TaskPriority = TaskPriority.NORMAL, dependencies: Optional[list] = None, metadata: Optional[dict] = None) -> Task: ...
    def get(self, task_id: str) -> Optional[Task]: ...
    def list(self, status: Optional[TaskStatus] = None, priority: Optional[TaskPriority] = None) -> list[Task]: ...
    def start(self, task_id: str) -> bool: ...
    def complete(self, task_id: str, result: Any = None) -> bool: ...
    def fail(self, task_id: str, error: str) -> bool: ...
    def cancel(self, task_id: str) -> bool: ...
    def next_task(self) -> Optional[Task]: ...
    def stats(self) -> dict: ...
```

#### 类: `TaxonomyClassifier`
```python
class TaxonomyClassifier:
    def classify(self, content: str) -> ClassificationResult: ...
```

#### 类: `KnowledgeTypeClassifier`
```python
class KnowledgeTypeClassifier:
    def classify(self, content: str) -> ClassificationResult: ...
```

#### 类: `AutoTagger`
```python
class AutoTagger:
    def tag(self, content: str) -> List[str]: ...
```

#### 类: `ClassificationEngine`
```python
class ClassificationEngine:
    def classify(self, content: str, taxonomy_only: bool = False) -> ClassificationResult: ...
    def classify_dual(self, content: str) -> Tuple[ClassificationResult, ClassificationResult]: ...
```

#### 全局函数
```python
def get_engine() -> ClassificationEngine: ...
def classify(content: str, taxonomy_only: bool = False) -> ClassificationResult: ...
def classify_dual(content: str) -> Tuple[ClassificationResult, ClassificationResult]: ...
def register_task(task_id: str, task: ClassificationTask): ...
def get_task(task_id: str) -> Optional[ClassificationTask]: ...
def get_stats() -> Dict[str, Any]: ...
```

---

## 5. cli — 命令行接口

### 5.1 路由 (`cli/router.py`)

#### 枚举: `ArgType`
```python
class ArgType(Enum):
    STRING = "string"
    INT = "int"
    FLOAT = "float"
    BOOL = "bool"
    LIST = "list"
```

#### 类: `Arg`
```python
@dataclass
class Arg:
    name: str
    arg_type: ArgType
    required: bool = False
    default: Any = None
    help: str = ""
    choices: List[str] = field(default_factory=list)
```

#### 类: `Command`
```python
@dataclass
class Command:
    name: str
    handler: Callable
    args: List[Arg] = field(default_factory=list)
    help: str = ""
    description: str = ""
```

#### 类: `ParsedArgs`
```python
@dataclass
class ParsedArgs:
    command: Optional[str] = None
    args: Dict[str, Any] = field(default_factory=dict)
    raw: List[str] = field(default_factory=list)
```

#### 类: `Router`
```python
class Router:
    def register(self, command: Command) -> None: ...
    def route(self, parsed: ParsedArgs) -> Any: ...
    def parse(self, tokens: List[str]) -> ParsedArgs: ...
    def execute(self, raw_input: str) -> Any: ...
```

#### 全局函数
```python
def get_router() -> Router: ...
def parse_and_run(raw_input: str) -> Any: ...
```

### 5.2 TUI (`cli/tui.py`)

#### 类: `Colors`
```python
class Colors:
    HEADER = ...
    OKBLUE = ...
    OKGREEN = ...
    WARNING = ...
    FAIL = ...
    ENDC = ...
    BOLD = ...
```

#### 类: `Column`
```python
@dataclass
class Column:
    name: str
    width: int
    align: str = "<"
```

#### 类: `Table`
```python
class Table:
    def __init__(self, columns: List[Column]): ...
    def add_row(self, values: List[Any]): ...
    def display(self): ...
```

#### 类: `ProgressBar`
```python
class ProgressBar:
    def __init__(self, total: int, description: str = ""): ...
    def update(self, current: int): ...
    def close(self): ...
```

#### 类: `Spinner`
```python
class Spinner:
    def __init__(self, message: str = ""): ...
    def update(self): ...
    def stop(self): ...
```

#### 类: `Pager`
```python
class Pager:
    def __init__(self, lines: List[str], page_size: int = 20): ...
    def display(self): ...
```

#### 函数
```python
def colorize(text: str, color: str) -> str: ...
def has_color_support() -> bool: ...
def print_header(text: str): ...
def print_success(text: str): ...
def print_error(text: str): ...
def print_warning(text: str): ...
def print_info(text: str): ...
def confirm(prompt: str) -> bool: ...
def input_with_default(prompt: str, default: str) -> str: ...
```

---

## 6. config — 配置加载

### 6.1 (`config/loader.py`)

#### 类: `ConfigLoader`
```python
class ConfigLoader:
    ENV_PREFIX = "MEMORY_HALL_"
    
    def __init__(self, config_path: Optional[str] = None): ...
    def load(self) -> Dict[str, Any]: ...
    def get(self, *keys, default=None) -> Any: ...
    def reload(self) -> Dict[str, Any]: ...
```

#### 全局函数
```python
def get_loader(config_path: Optional[str] = None) -> ConfigLoader: ...
def load_config() -> Dict[str, Any]: ...
def get_config(*keys, default=None) -> Any: ...
```

---

## 7. evolve — 自进化引擎

### 7.1 自进化 (`evolve/self_evolution.py`)

#### 枚举: `ConfidenceLevel`
```python
class ConfidenceLevel(Enum):
    HIGH = "high"
    MEDIUM = "medium"
    LOW = "low"
```

#### 类: `GenerationItem`
```python
@dataclass
class GenerationItem:
    content: str
    confidence: ConfidenceLevel
    timestamp: float
    source: str = ""
```

#### 类: `VerificationResult`
```python
@dataclass
class VerificationResult:
    verified: bool
    confidence: float
    issues: List[str] = field(default_factory=list)
```

#### 类: `CorrectionResult`
```python
@dataclass
class CorrectionResult:
    corrected: bool
    original: str
    corrected_content: str
    explanation: str
```

#### 类: `Intent`
```python
@dataclass
class Intent:
    action: str
    target: str
    parameters: Dict[str, Any] = field(default_factory=dict)
    confidence: float = 0.0
```

#### 类: `RootCauseAnalysis`
```python
@dataclass
class RootCauseAnalysis:
    problem: str
    root_cause: str
    confidence: float
    suggestions: List[str] = field(default_factory=list)
```

#### 类: `FixExecutionResult`
```python
@dataclass
class FixExecutionResult:
    success: bool
    applied: bool
    result: str
    rollback_available: bool
```

#### 类: `ProactiveKnowledgeCorrector`
```python
class ProactiveKnowledgeCorrector:
    def correct(self, content: str) -> CorrectionResult: ...
    def verify_correction(self, original: str, corrected: str) -> VerificationResult: ...
```

#### 类: `IntentPredictor`
```python
class IntentPredictor:
    def predict(self, context: Dict[str, Any]) -> Intent: ...
```

#### 类: `AutonomousRepairExecutor`
```python
class AutonomousRepairExecutor:
    def execute_fix(self, fix_plan: RootCauseAnalysis) -> FixExecutionResult: ...
    def rollback(self) -> bool: ...
```

#### 类: `AutomatedRootCauseFixer`
```python
class AutomatedRootCauseFixer:
    def analyze(self, error_log: str) -> RootCauseAnalysis: ...
    def auto_fix(self, analysis: RootCauseAnalysis) -> FixExecutionResult: ...
```

### 7.2 三环架构 (`evolve/three_ring_architecture.py`)

#### 枚举: `RingStatus`
```python
class RingStatus(Enum):
    IDLE = "idle"
    ACTIVE = "active"
    ERROR = "error"
```

#### 枚举: `AnomalyType`
```python
class AnomalyType(Enum):
    LATENCY_SPIKE = "latency_spike"
    ERROR_RATE_INCREASE = "error_rate_increase"
    RESOURCE_EXHAUSTION = "resource_exhaustion"
    BEHAVIORAL_DRIFT = "behavioral_drift"
```

#### 类: `MonitorEvent`
```python
@dataclass
class MonitorEvent:
    event_type: AnomalyType
    severity: str
    data: Dict[str, Any]
    timestamp: float
```

#### 类: `DecisionOutput`
```python
@dataclass
class DecisionOutput:
    decision: str
    confidence: float
    reasoning: str
```

#### 类: `ExecutionOutput`
```python
@dataclass
class ExecutionOutput:
    action: str
    success: bool
    result: Any
    error: Optional[str]
```

#### 类: `MonitorRing`
```python
class MonitorRing:
    def observe(self) -> MonitorEvent: ...
    def is_anomaly(self, event: MonitorEvent) -> bool: ...
```

#### 类: `DecisionRing`
```python
class DecisionRing:
    def decide(self, event: MonitorEvent) -> DecisionOutput: ...
```

#### 类: `ExecutionRing`
```python
class ExecutionRing:
    def execute(self, decision: DecisionOutput) -> ExecutionOutput: ...
```

#### 类: `ThreeRingClosedLoop`
```python
class ThreeRingClosedLoop:
    def run_cycle(self) -> ExecutionOutput: ...
    def get_status(self) -> Dict[str, Any]: ...
```

---

## 8. extractor — 内容提取

### 8.1 (`extractor/extractor.py`)

#### 枚举: `MemoryType`
```python
class MemoryType(Enum):
    EPISODIC = "episodic"     # 情景记忆
    SEMANTIC = "semantic"     # 语义记忆
    PROCEDURAL = "procedural" # 程序性记忆
    ABSTRACT = "abstract"     # 抽象知识
```

#### 类: `CompressionResult`
```python
@dataclass
class CompressionResult:
    original_length: int
    compressed_length: int
    compression_ratio: float
    memory_type: MemoryType
    quality_score: float
    processing_time_ms: float
```

#### 类: `Extractor`
```python
class Extractor:
    def extract(self, content: str) -> CompressionResult: ...
    def batch_extract(self, contents: List[str]) -> List[CompressionResult]: ...
```

#### 类: `SimpleLLMClient`
```python
class SimpleLLMClient:
    def complete(self, prompt: str, max_tokens: int = 100) -> str: ...
```

### 8.2 自适应压缩 (`extractor/adaptive_compression.py`)

#### 枚举: `CompressionLevel`
```python
class CompressionLevel(Enum):
    MINIMAL = "minimal"     # 最小压缩
    BALANCED = "balanced"   # 平衡压缩
    AGGRESSIVE = "aggressive" # 激进压缩
```

#### 类: `AdaptiveThresholds`
```python
@dataclass
class AdaptiveThresholds:
    quality_warning: float = 0.7
    quality_critical: float = 0.5
    ratio_min: float = 0.3
    ratio_max: float = 0.8
```

#### 类: `CompressionContext`
```python
@dataclass
class CompressionContext:
    content: str
    level: CompressionLevel
    thresholds: AdaptiveThresholds
    metadata: Dict[str, Any] = field(default_factory=dict)
```

#### 类: `AdaptiveCompressionController`
```python
class AdaptiveCompressionController:
    def select_level(self, content: str) -> CompressionLevel: ...
    def adjust_thresholds(self, feedback: Dict[str, Any]) -> AdaptiveThresholds: ...
```

#### 类: `AdaptiveExtractionPipeline`
```python
class AdaptiveExtractionPipeline:
    def run(self, content: str) -> CompressionResult: ...
    def adapt(self, feedback: Dict[str, Any]): ...
```

---

## 9. fence — 围栏隔离

### 9.1 (`fence/fence.py`)

#### 枚举: `SpaceType`
```python
class SpaceType(Enum):
    PRIVATE = "private"  # 私人空间
    LIBRARY = "library"  # 图书馆
    PUBLIC = "public"    # 公共空间
```

#### 枚举: `Permission`
```python
class Permission(Enum):
    NONE = 0
    READ = 1
    WRITE = 2
    ADMIN = 3
```

#### 类: `SpaceBoundary`
```python
@dataclass
class SpaceBoundary:
    space_type: SpaceType
    path: str
    permission: Permission
    parent_space: Optional['SpaceBoundary'] = None
    allowed_extensions: List[str] = field(default_factory=list)
    max_size_mb: int = 100
```

#### 类: `ViolationEvent`
```python
@dataclass
class ViolationEvent:
    timestamp: float
    source_space: SpaceType
    target_space: SpaceType
    action: str
    details: str
    severity: str  # low, medium, high, critical
```

#### 类: `FenceAlert`
```python
class FenceAlert:
    def __init__(self): ...
    def register_handler(self, handler: Callable[[ViolationEvent], None]): ...
    def trigger(self, event: ViolationEvent): ...
    def get_recent_violations(self, limit: int = 50) -> List[Dict]: ...
```

#### 类: `MemoryPalaceFence`
```python
class MemoryPalaceFence:
    VERSION = "1.3.0"
    
    def __init__(self, base_path: Optional[str] = None): ...
    
    # ---- 公共API ----
    def set_current_context(self, user: str, space: SpaceType): ...
    def check_boundary(self, path: str, action: str = "access") -> tuple[bool, Optional[ViolationEvent]]: ...
    def validate_access(self, path: str, action: str = "read") -> bool: ...
    def enforce_isolation(self) -> Dict[str, Any]: ...
    def get_fence_status(self) -> Dict[str, Any]: ...
```

#### 全局函数
```python
def get_fence() -> MemoryPalaceFence: ...
def check_boundary(path: str, action: str = "access") -> tuple[bool, Optional[ViolationEvent]]: ...
def validate_access(path: str, action: str = "read") -> bool: ...
```

---

## 10. gateway — 网关

### 10.1 (`gateway/gateway.py`)

#### 类: `Config`
```python
class Config:
    _instance = None
    
    def __new__(cls): ...
    def get(self, key: str, default=None): ...
    def reload(self): ...
```

#### 类: `LRUCache`
```python
class LRUCache:
    def __init__(self, max_size: int = None, ttl_days: int = None): ...
    
    def get(self, key: str) -> Optional[Any]: ...
    def set(self, key: str, value: Any): ...
    def invalidate(self, key: str = None): ...
    def get_stats(self) -> dict: ...
```

#### 类: `Gateway`
```python
class Gateway:
    def __init__(self): ...
    
    def put(self, content: str, record_type: str = "general", user: str = "system") -> Dict: ...
    def get(self, record_id: str = None, filepath: str = None) -> Optional[Dict]: ...
    def find(self, query: str, limit: int = 10) -> List[Dict]: ...
    def remove(self, record_id: str = None, filepath: str = None) -> bool: ...
    def logs(self, limit: int = 100) -> List[Dict]: ...
    def stats(self) -> dict: ...
```

#### 模块级函数
```python
def write(content: str, record_type: str = "general", user: str = "system", metadata: Dict = None, notify: bool = True) -> Dict[str, Any]: ...
def read(record_id: str = None, filepath: str = None) -> Optional[Dict]: ...
def search(query: str, record_type: str = None, limit: int = 10) -> List[Dict]: ...
def delete(record_id: str = None, filepath: str = None) -> bool: ...
def get_audit_logs(limit: int = 100) -> List[Dict]: ...
def get_cache_stats() -> dict: ...
def clear_cache(): ...
def audit_log(action: str, user: str, details: Dict = None): ...
```

---

## 11. health — 健康检查

### 11.1 枚举 (`health/enums.py`)
```python
class HealthStatus(Enum):
    OK = "ok"
    WARNING = "warning"
    ALERT = "alert"
    CRITICAL = "critical"

class CircuitState(Enum):
    CLOSED = "closed"
    OPEN = "open"
    HALF_OPEN = "half_open"
```

### 11.2 数据类 (`health/data_classes.py`)
```python
@dataclass
class SixDimensionData:
    task_success_rate: float
    steps_per_task_p95: float
    token_per_task_p95: float
    tool_failure_rate: float
    verification_pass_rate: float
    latency_p50_ms: float
    latency_p95_ms: float
    latency_p99_ms: float
    timestamp: str

@dataclass
class CircuitBreakerInfo:
    name: str
    state: CircuitState
    failure_count: int
    success_count: int
    last_failure_time: Optional[float]

@dataclass
class DiagnosisResult:
    dimension: str
    status: HealthStatus
    current_value: float
    threshold_warning: float
    threshold_critical: float
    trend: str
    root_cause: str
    suggestions: List[str]
    timestamp: str
```

### 11.3 指标收集 (`health/metrics.py`)
```python
class SixDimensionMetrics:
    def record_task(self, task_data: Dict[str, Any]): ...
    def record_tool_failure(self, tool_name: str, error: str = ""): ...
    def get_current_metrics(self) -> SixDimensionData: ...
    def get_history_for_adaptive(self) -> List[SixDimensionData]: ...
    def get_stats(self) -> Dict[str, Any]: ...
```

### 11.4 断路器 (`health/circuit_breaker.py`)
```python
class CircuitBreaker:
    def __init__(self, name: str, failure_threshold: int = 5, success_threshold: int = 2, timeout: float = 30.0): ...
    def call(self, func: Callable, *args, **kwargs) -> Any: ...
    def get_state(self) -> CircuitState: ...
    def reset(self): ...
```

### 11.5 断路器面板 (`health/panel.py`)
```python
class CircuitBreakerPanel:
    def register(self, name: str, breaker: CircuitBreaker): ...
    def get_panel_status(self) -> Dict[str, Any]: ...
    def reset_all(self): ...
```

### 11.6 诊断引擎 (`health/diagnostic.py`)
```python
class DiagnosticEngine:
    def diagnose(self, current: SixDimensionData, history: List[SixDimensionData]) -> List[DiagnosisResult]: ...
    def save_diagnosis(self, diagnoses: List[DiagnosisResult]): ...
```

### 11.7 阈值计算 (`health/threshold.py`)
```python
class AdaptiveThresholdCalculator:
    def calculate(self, dimension: str, values: List[float]) -> Tuple[float, float]: ...
```

### 11.8 主检查器 (`health/checker.py`)
```python
class HealthChecker:
    def get_full_report(self) -> Dict[str, Any]: ...
    def get_quick_status(self) -> Dict[str, Any]: ...
    def record_task_completion(self, success: bool, steps: int, tokens: int, latency_ms: float, verified: bool = False): ...
    def record_failure(self, tool_name: str, error: str = ""): ...
```

---

## 12. integrate — 集成系统

### 12.1 (`integrate/__init__.py`)

#### 类: `MemoryPalaceIntegration`
```python
class MemoryPalaceIntegration:
    def __init__(self, config: Optional['IntegrationConfig'] = None): ...
    
    async def initialize(self): ...
    async def write_memory(self, content: str, memory_type: str = "general", metadata: Dict = None) -> Dict: ...
    async def read_memory(self, record_id: str) -> Optional[Dict]: ...
    async def search_memory(self, query: str, limit: int = 10) -> List[Dict]: ...
    async def delete_memory(self, record_id: str) -> bool: ...
    def get_stats(self) -> Dict[str, Any]: ...
    def health_check(self) -> Dict[str, Any]: ...
```

### 12.2 配置 (`integrate/config.py`)
```python
@dataclass
class IntegrationConfig:
    gateway_enabled: bool = True
    wal_enabled: bool = True
    fence_enabled: bool = False
    sensory_enabled: bool = True
    extractor_enabled: bool = True
    repair_enabled: bool = True
    evolve_enabled: bool = False
    memory_dir: str = "~/.openclaw/memory-vault"
    wal_dir: str = "~/.openclaw/memory-vault/wal"
```

### 12.3 统计 (`integrate/stats.py`)
```python
@dataclass
class MemoryStats:
    total_records: int
    total_size_bytes: int
    cache_hit_rate: float
    wal_size_bytes: int
    last_backup: Optional[float]
```

---

## 13. interfaces — 接口抽象

### 13.1 记忆殿堂接口 (`interfaces/imemory_vault.py`)

#### 类: `SearchResult`
```python
@dataclass
class SearchResult:
    id: str
    score: float
    content: str
    metadata: Dict = None
```

#### 类: `IMemoryVault` (ABC)
```python
class IMemoryVault(ABC):
    @abstractmethod
    async def read(self, key: str) -> Optional[Any]: ...
    
    @abstractmethod
    async def write(self, key: str, value: Any, metadata: Dict = None) -> bool: ...
    
    @abstractmethod
    async def delete(self, key: str) -> bool: ...
    
    @abstractmethod
    async def search(self, query: str, limit: int = 10) -> List[SearchResult]: ...
    
    @abstractmethod
    async def list_keys(self) -> List[str]: ...
```

### 13.2 文件系统适配器 (`interfaces/adapters/file_system_adapter.py`)
```python
class FileSystemAdapter(IMemoryVault):
    def __init__(self, vault_dir: str = None): ...
    
    async def read(self, key: str) -> Optional[Any]: ...
    async def write(self, key: str, value: Any, metadata: Dict = None) -> bool: ...
    async def delete(self, key: str) -> bool: ...
    async def search(self, query: str, limit: int = 10) -> List[SearchResult]: ...
    async def list_keys(self) -> List[str]: ...
```

### 13.3 验证脚本 (`interfaces/verify_interface.py`)
```python
def verify_interface() -> bool: ...
```

---

## 14. introspection — 自省模块

### 14.1 模块扫描 (`introspection/modules.py`)
```python
# 自动生成模块数据（111个模块）

def get_modules() -> List[str]: ...
def get_module(module_id: str) -> Optional[Dict[str, Any]]: ...
def get_dependencies(module_id: str) -> List[str]: ...
def get_stats() -> Dict[str, int]: ...
```

### 14.2 依赖图 (`introspection/dependency_graph.py`)
```python
@dataclass
class DependencyEdge:
    from_module: str
    to_module: str
    edge_type: str  # "import", "function_call", etc.

class DependencyGraph:
    def __init__(self): ...
    def add_module(self, module_id: str, classes: List[str], functions: List[str], imports: List[str]): ...
    def get_edges(self) -> List[DependencyEdge]: ...
    def get_dependents(self, module_id: str) -> List[str]: ...
    def visualize(self) -> Dict[str, Any]: ...
```

### 14.3 状态API (`introspection/status_api.py`)
```python
class ModuleStatus(Enum):
    HEALTHY = "healthy"
    DEGRADED = "degraded"
    DOWN = "down"

class HealthCheckResult:
    module: str
    status: ModuleStatus
    latency_ms: float
    details: Dict[str, Any]

class StatusAPI:
    def get_module_status(self, module_id: str) -> HealthCheckResult: ...
    def get_all_status(self) -> List[HealthCheckResult]: ...
    def get_system_health(self) -> Dict[str, Any]: ...
```

### 14.4 问题检测 (`introspection/problem_detector.py`)
```python
class ProblemSeverity(Enum):
    LOW = "low"
    MEDIUM = "medium"
    HIGH = "high"
    CRITICAL = "critical"

class ProblemCategory(Enum):
    CIRCULAR_DEPENDENCY = "circular_dependency"
    MISSING_IMPORT = "missing_import"
    SYNTAX_ERROR = "syntax_error"
    TYPE_ERROR = "type_error"

@dataclass
class Problem:
    severity: ProblemSeverity
    category: ProblemCategory
    module: str
    message: str
    line_number: Optional[int]

@dataclass
class ProblemReport:
    problems: List[Problem]
    summary: Dict[str, int]

class ProblemDetector:
    def scan(self) -> ProblemReport: ...
    def detect_circular_dependencies(self) -> List[List[str]]: ...
```

### 14.5 模块映射生成器 (`introspection/module_map_generator.py`)
```python
@dataclass
class ModuleInterface:
    module_id: str
    path: str
    classes: List[str]
    functions: List[str]
    imports: List[str]

class ModuleMapGenerator:
    def generate(self) -> Dict[str, ModuleInterface]: ...
    def save_to_file(self, filepath: str): ...
```

---

## 15. memory_layer — 记忆层级

### 15.1 (`memory_layer/rl_access.py`)

#### 枚举: `MemoryTier`
```python
class MemoryTier(Enum):
    HOT = "hot"     # 热记忆
    WARM = "warm"   # 温记忆
    COLD = "cold"   # 冷记忆
```

#### 枚举: `MemoryAccessAction`
```python
class MemoryAccessAction(Enum):
    PROMOTE_TO_HOT = "promote_to_hot"
    DEMOTE_TO_WARM = "demote_to_warm"
    DEMOTE_TO_COLD = "demote_to_cold"
    KEEP = "keep"
```

#### 类: `MemoryAccessState`
```python
@dataclass
class MemoryAccessState:
    access_frequency: float = 0.0
    recency: float = 0.0
    importance_score: float = 0.5
    access_velocity: float = 0.0
    time_of_day: int = 12
    
    def to_vector(self) -> List[float]: ...
```

#### 类: `MemoryBlock`
```python
@dataclass
class MemoryBlock:
    key: str
    value: Any
    tier: MemoryTier = MemoryTier.WARM
    created_at: float = field(default_factory=time.time)
    last_access: float = field(default_factory=time.time)
    access_count: int = 0
    importance: float = 0.5
    access_history: List[float] = field(default_factory=list)
    
    def record_access(self): ...
```

#### 类: `SimplePolicyNetwork`
```python
class SimplePolicyNetwork:
    def predict_q(self, state: MemoryAccessState, action: MemoryAccessAction) -> float: ...
    def choose_action(self, state: MemoryAccessState, epsilon: float = 0.1) -> MemoryAccessAction: ...
    def update(self, state: MemoryAccessState, action: MemoryAccessAction, reward: float, next_state: MemoryAccessState, learning_rate: float = 0.1): ...
```

#### 类: `RLMemoryLayerManager`
```python
class RLMemoryLayerManager:
    def __init__(
        self,
        hot_size: int = 100,
        warm_size: int = 500,
        cold_size: int = 2000,
        training_interval: int = 100,
    ): ...
    
    def on_access(self, key: str, default_value: Any = None) -> tuple[Any, bool, float]: ...
    def add(self, key: str, value: Any, importance: float = 0.5): ...
    def get_stats(self) -> Dict[str, Any]: ...
    def get_tier_distribution(self) -> Dict[str, int]: ...
```

---

## 16. mini_agent — 轻量代理

### 16.1 钩子系统 (`mini_agent/hooks.py`)

#### 异常: `TimeoutError`
```python
class TimeoutError(Exception): ...
```

#### 装饰器: `timeout`
```python
@timeout(seconds: float)
def func(...): ...
```

#### 类: `HookEvent`
```python
@dataclass
class HookEvent:
    event_type: str
    timestamp: float
    data: Dict[str, Any]
```

#### 类: `HookResult`
```python
@dataclass
class HookResult:
    success: bool
    modified: bool
    data: Any
    error: Optional[str]
```

#### 类: `ToolCall`
```python
@dataclass
class ToolCall:
    tool_name: str
    arguments: Dict[str, Any]
    timestamp: float
```

#### 类: `ToolResult`
```python
@dataclass
class ToolResult:
    tool_name: str
    result: Any
    error: Optional[str]
    duration_ms: float
```

#### 类: `HookContext`
```python
class HookContext:
    session_id: str
    agent_id: str
    metadata: Dict[str, Any]
```

#### 类: `BeforeToolCallHook`
```python
class BeforeToolCallHook(ABC):
    @abstractmethod
    async def before_tool_call(self, context: HookContext, tool_call: ToolCall) -> HookResult: ...
```

#### 类: `ToolResultPersistHook`
```python
class ToolResultPersistHook(ABC):
    @abstractmethod
    async def on_tool_result(self, context: HookContext, tool_result: ToolResult) -> HookResult: ...
```

#### 类: `HookManager`
```python
class HookManager:
    def register_before_tool_hook(self, hook: BeforeToolCallHook, priority: int = 0): ...
    def register_result_hook(self, hook: ToolResultPersistHook, priority: int = 0): ...
    async def execute_before_hooks(self, context: HookContext, tool_call: ToolCall) -> List[HookResult]: ...
    async def execute_result_hooks(self, context: HookContext, tool_result: ToolResult) -> List[HookResult]: ...
```

#### 全局函数
```python
def create_default_hook_manager() -> HookManager: ...
```

### 16.2 压缩 (`mini_agent/compact.py`)

#### 类: `CompactionConfig`
```python
@dataclass
class CompactionConfig:
    max_tokens: int = 60000
    summary_ratio: float = 0.1
    preserve_recent: int = 10
    min_compact_length: int = 2000
```

#### 类: `CompactionResult`
```python
@dataclass
class CompactionResult:
    original_count: int
    compacted_count: int
    summary_length: int
    removed_count: int
```

#### 类: `Message`
```python
@dataclass
class Message:
    role: str
    content: str
    timestamp: float = field(default_factory=time.time)
```

#### 函数
```python
def estimate_message_tokens(message: Message) -> int: ...
def estimate_session_tokens(messages: List[Message]) -> int: ...
def should_compact(messages: List[Message], config: CompactionConfig) -> bool: ...
def compact_session(messages: List[Message], config: CompactionConfig) -> List[Message]: ...
```

### 16.3 任务注册表 (`mini_agent/registry.py`)

（同agent模块中的TaskRegistry，参见1.4节）

---

## 17. normalizer — 归一化

### 17.1 (`normalizer/deduplicator.py`)

#### 类: `SimHash`
```python
class SimHash:
    def __init__(self, content: str, hash_size: int = 64): ...
    def compute(self) -> int: ...
    @staticmethod
    def hamming_distance(hash1: int, hash2: int) -> int: ...
```

#### 类: `LLMSemanticDeduplicator`
```python
class LLMSemanticDeduplicator:
    def __init__(self, threshold: float = 0.85): ...
    def is_duplicate(self, text1: str, text2: str) -> Tuple[bool, float]: ...
    def find_duplicates(self, texts: List[str]) -> List[Tuple[int, int, float]]: ...
```

#### 类: `Deduplicator`
```python
class Deduplicator:
    def add(self, key: str, content: str): ...
    def is_duplicate(self, content: str) -> Tuple[bool, Optional[str]]: ...
    def get_stats(self) -> Dict[str, Any]: ...
    def clear(self): ...
```

---

## 18. optimize — 优化器

### 18.1 (`optimize/adaptive_compression.py`)

#### 枚举: `CompressionLevel`
```python
class CompressionLevel(Enum):
    MINIMAL = "minimal"
    BALANCED = "balanced"
    AGGRESSIVE = "aggressive"
    ULTRA = "ultra"
```

#### 类: `CompressionTask`
```python
@dataclass
class CompressionTask:
    task_id: str
    content: str
    level: CompressionLevel
    priority: int = 0
    created_at: float = field(default_factory=time.time)
```

#### 类: `MemoryEntry`
```python
@dataclass
class MemoryEntry:
    key: str
    content: str
    compressed: bool = False
    compression_ratio: float = 1.0
    last_access: float = field(default_factory=time.time)
    access_count: int = 0
    importance: float = 0.5
```

#### 类: `CompressionResult`
```python
@dataclass
class CompressionResult:
    key: str
    original_size: int
    compressed_size: int
    ratio: float
    processing_time_ms: float
    success: bool
```

#### 类: `IndexSearchResult`
```python
@dataclass
class IndexSearchResult:
    key: str
    score: float
    content: str
```

#### 类: `AdaptiveCompressionScheduler`
```python
class AdaptiveCompressionScheduler:
    def schedule(self, entry: MemoryEntry) -> Optional[CompressionTask]: ...
    def should_compress(self, entry: MemoryEntry) -> bool: ...
    def get_queue_size(self) -> int: ...
```

#### 类: `IncrementalMemoryIndex`
```python
class IncrementalMemoryIndex:
    def add(self, key: str, content: str, metadata: Dict = None): ...
    def search(self, query: str, limit: int = 10) -> List[IndexSearchResult]: ...
    def remove(self, key: str): ...
    def update(self, key: str, content: str): ...
```

#### 类: `PredictiveCompressor`
```python
class PredictiveCompressor:
    def predict_access(self, key: str) -> float: ...  # 返回访问概率
    def suggest_compression_level(self, key: str) -> CompressionLevel: ...
    def get_hot_keys(self, top_n: int = 10) -> List[str]: ...
```

---

## 19. permission — 权限引擎

### 19.1 (`permission/engine.py`)

#### 枚举: `PermissionLevel` (IntEnum)
```python
class PermissionLevel(IntEnum):
    READONLY = 1       # 只读
    WORKSPACE_WRITE = 2  # 工作区写入
    DANGER_FULL_ACCESS = 3  # 危险完全访问
    PROMPT = 4        # 需要确认
    ALLOW = 5         # 完全允许
```

#### 枚举: `RuleAction` (IntEnum)
```python
class RuleAction(IntEnum):
    ALLOW = 1
    DENY = 2
    ASK = 3
```

#### 类: `Rule`
```python
@dataclass
class Rule:
    pattern: str                    # 正则表达式
    action: RuleAction
    min_level: PermissionLevel = PermissionLevel.READONLY
    description: str = ""
```

#### 类: `PermissionContext`
```python
@dataclass
class PermissionContext:
    operation: str                  # read, write, delete, exec, etc.
    target: str                     # 目标路径/资源
    requested_by: str = "system"
    metadata: dict = field(default_factory=dict)
```

#### 类: `PermissionResult`
```python
@dataclass
class PermissionResult:
    allowed: bool
    action: RuleAction
    required_level: PermissionLevel
    message: str
    requires_confirmation: bool = False
```

#### 类: `PermissionEngine`
```python
class PermissionEngine:
    def __init__(self): ...
    
    def check(self, context: PermissionContext, user_level: PermissionLevel) -> PermissionResult: ...
    def add_rule(self, rule: Rule) -> None: ...
    def remove_rule(self, pattern: str) -> bool: ...
    def get_user_level(self, user: str) -> PermissionLevel: ...
    def set_user_level(self, user: str, level: PermissionLevel) -> None: ...
```

#### 全局函数
```python
def get_engine() -> PermissionEngine: ...
def check_permission(operation: str, target: str, user: str = "system") -> PermissionResult: ...
```

---

## 20. pipeline — 流水线

### 20.1 (`pipeline/cross_reference.py`)

#### 枚举: `SimilarityMethod`
```python
class SimilarityMethod(Enum):
    EXACT = "exact"
    SEMANTIC = "semantic"
    STRUCTURAL = "structural"
    FUZZY = "fuzzy"
```

#### 类: `Capsule`
```python
@dataclass
class Capsule:
    capsule_id: str
    content: str
    capsule_type: str
    metadata: Dict[str, Any] = field(default_factory=dict)
    created_at: float = field(default_factory=time.time)
```

#### 类: `CrossReferenceResult`
```python
@dataclass
class CrossReferenceResult:
    source_id: str
    target_id: str
    similarity: float
    method: SimilarityMethod
    details: Dict[str, Any] = field(default_factory=dict)
```

#### 类: `CrossReferenceEngine`
```python
class CrossReferenceEngine:
    def add_capsule(self, capsule: Capsule): ...
    def find_references(self, capsule_id: str, method: SimilarityMethod = SimilarityMethod.SEMANTIC, threshold: float = 0.7) -> List[CrossReferenceResult]: ...
    def find_all_references(self, threshold: float = 0.7) -> List[CrossReferenceResult]: ...
    def get_stats(self) -> Dict[str, Any]: ...
```

#### 全局函数
```python
def get_engine() -> CrossReferenceEngine: ...
def compute_cross_references(capsules: List[Capsule], threshold: float = 0.7) -> List[CrossReferenceResult]: ...
```

---

## 21. plugin — 插件系统

### 21.1 (`plugin/plugin.py`)

#### 枚举: `PluginState`
```python
class PluginState(Enum):
    UNLOADED = "unloaded"
    LOADING = "loading"
    LOADED = "loaded"
    ACTIVE = "active"
    ERROR = "error"
    DISABLED = "disabled"
```

#### 类: `PluginMetadata`
```python
@dataclass
class PluginMetadata:
    id: str
    name: str
    version: str
    description: str = ""
    author: str = ""
    dependencies: List[str] = field(default_factory=list)
    entry_point: str = "main"
```

#### 类: `PluginInterface` (ABC)
```python
class PluginInterface(ABC):
    @abstractmethod
    def get_metadata(self) -> PluginMetadata: ...
    
    @abstractmethod
    def initialize(self, config: Dict[str, Any]) -> None: ...
    
    @abstractmethod
    def activate(self) -> None: ...
    
    @abstractmethod
    def deactivate(self) -> None: ...
```

#### 类: `PluginRegistry`
```python
class PluginRegistry:
    def register(self, plugin: PluginInterface) -> bool: ...
    def unregister(self, plugin_id: str) -> bool: ...
    def get(self, plugin_id: str) -> Optional[PluginInterface]: ...
    def list_all(self) -> List[PluginMetadata]: ...
    def list_active(self) -> List[PluginInterface]: ...
    def load(self, plugin_id: str) -> bool: ...
    def unload(self, plugin_id: str) -> bool: ...
```

#### 类: `PluginLoader`
```python
class PluginLoader:
    def discover_plugins(self, plugin_dir: str) -> List[PluginMetadata]: ...
    def load_from_file(self, filepath: str) -> Optional[PluginInterface]: ...
    def load_from_module(self, module_name: str) -> Optional[PluginInterface]: ...
```

#### 装饰器
```python
def plugin_metadata(id: str, name: str, version: str, **kwargs) -> Callable: ...
```

---

## 22. repair — 备份修复

### 22.1 (`repair/backup_manager.py`)

#### 枚举: `VerificationStatus`
```python
class VerificationStatus(Enum):
    VERIFIED = "verified"
    UNVERIFIED = "unverified"
    HIGH_CONFIDENCE = "high"
    LOW_CONFIDENCE = "low"
    FAILED = "failed"
```

#### 枚举: `ImportanceLevel`
```python
class ImportanceLevel(Enum):
    CRITICAL = 1.0
    HIGH = 0.9
    MEDIUM = 0.7
    LOW = 0.5
    MINIMAL = 0.3
```

#### 类: `BackupSnapshot`
```python
@dataclass
class BackupSnapshot:
    snapshot_id: str
    session_id: str
    timestamp: float
    critical_keys: List[str]
    memory_state: Dict[str, Any]
    metadata: Dict[str, Any] = field(default_factory=dict)
    checksum: Optional[str] = None
    compressed: bool = False
```

#### 类: `RestorationResult`
```python
@dataclass
class RestorationResult:
    snapshot_id: str
    restored_keys: List[str]
    missing_keys: List[str]
    restored_size_bytes: int
    restoration_time_ms: float
    success: bool
    message: str = ""
```

#### 类: `RAGVerificationResult`
```python
@dataclass
class RAGVerificationResult:
    verified: bool
    confidence: float
    status: VerificationStatus
    source_uri: Optional[str] = None
    source_text: Optional[str] = None
    matched_content: Optional[str] = None
    similarity: float = 0.0
    verification_time_ms: float = 0.0
    claim: str = ""
```

#### 类: `MemoryBackupManager`
```python
class MemoryBackupManager:
    def create_snapshot(self, session_id: str, keys: List[str]) -> BackupSnapshot: ...
    def restore_snapshot(self, snapshot: BackupSnapshot) -> RestorationResult: ...
    def list_snapshots(self, session_id: Optional[str] = None) -> List[BackupSnapshot]: ...
    def delete_snapshot(self, snapshot_id: str) -> bool: ...
```

#### 类: `ImportanceAwareCompressor`
```python
class ImportanceAwareCompressor:
    def compress(self, content: str, importance: ImportanceLevel) -> str: ...
    def decompress(self, compressed: str) -> str: ...
    def batch_compress(self, items: List[Tuple[str, ImportanceLevel]]) -> Dict[str, str]: ...
```

---

## 23. sensory — 感知引擎

### 23.1 (`sensory/semantic_search.py`)

#### 类: `GatewayConfig`
```python
class GatewayConfig:
    # 向量索引配置
    vector:
        dimension: int = 384
        metric: str = "cosine"
        index_type: str = "hnsw"
    
    # 搜索配置
    search:
        default_limit: int = 10
        max_limit: int = 100
        timeout_seconds: int = 30
        min_score: float = 0.0
        rerank: bool = True
    
    # 缓存配置
    cache:
        enabled: bool = True
        max_size: int = 1000
        ttl_seconds: int = 3600
    
    # 压缩配置
    compression:
        enabled: bool = True
        precision: str = "float16"
```

#### 枚举: `TaskStatus`
```python
class TaskStatus(Enum):
    PENDING = "pending"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"
    CANCELLED = "cancelled"
    TIMEOUT = "timeout"
```

#### 类: `SearchQuery`
```python
@dataclass
class SearchQuery:
    query: str
    limit: int = 10
    offset: int = 0
    min_score: float = 0.0
    filters: Dict[str, Any] = field(default_factory=dict)
    include_vectors: bool = False
    timeout: Optional[float] = None
```

#### 类: `SearchResult`
```python
@dataclass
class SearchResult:
    id: str
    score: float
    content: str
    metadata: Dict[str, Any] = field(default_factory=dict)
    vector: Optional[List[float]] = None
    rank: int = 0
    latency_ms: float = 0.0
    
    def to_dict(self) -> Dict[str, Any]: ...
```

#### 类: `QueryTask`
```python
@dataclass
class QueryTask:
    task_id: str
    query: SearchQuery
    status: TaskStatus = TaskStatus.PENDING
    created_at: float = field(default_factory=time.time)
    started_at: Optional[float] = None
    completed_at: Optional[float] = None
    results: List[SearchResult] = field(default_factory=list)
    error: Optional[str] = None
    total_hits: int = 0
    page_token: Optional[str] = None
    
    def duration_ms(self) -> float: ...
    def to_dict(self) -> Dict[str, Any]: ...
```

#### 类: `TaskRegistry`
```python
class TaskRegistry:
    def __init__(self, max_tasks: int = 1000, ttl_seconds: int = 3600): ...
    def create_task(self, query: SearchQuery) -> QueryTask: ...
    def get_task(self, task_id: str) -> Optional[QueryTask]: ...
    def update_status(self, task_id: str, status: TaskStatus, results: List[SearchResult] = None, error: str = None) -> bool: ...
    def register_callback(self, task_id: str, callback: Callable[[QueryTask], None]): ...
    def cancel_task(self, task_id: str) -> bool: ...
    def get_stats(self) -> Dict[str, Any]: ...
```

#### 类: `VectorIndex`
```python
class VectorIndex:
    def __init__(self, dimension: int = None, metric: str = "cosine", compression: bool = True, precision: str = "float16"): ...
    def add(self, id: str, vector: List[float], metadata: Dict[str, Any] = None) -> bool: ...
    def get(self, id: str) -> Optional[List[float]]: ...
    def search(self, query_vector: List[float], limit: int = 10, min_score: float = 0.0) -> List[Tuple[str, float]]: ...
    def delete(self, id: str) -> bool: ...
    def get_stats(self) -> Dict[str, Any]: ...
```

#### 类: `SemanticSearchEngine`
```python
class SemanticSearchEngine:
    def __init__(self, index: VectorIndex = None, task_registry: TaskRegistry = None, gateway_config: GatewayConfig = None): ...
    
    def add_document(self, id: str, content: str, vector: List[float] = None, metadata: Dict[str, Any] = None) -> bool: ...
    
    def search(
        self,
        query: str,
        limit: int = 10,
        offset: int = 0,
        min_score: float = 0.0,
        filters: Dict[str, Any] = None,
        timeout: float = None,
        include_vectors: bool = False,
    ) -> Tuple[List[SearchResult], int, Optional[str]]: ...
    
    async def search_async(self, query: str, limit: int = 10, offset: int = 0, **kwargs) -> Tuple[List[SearchResult], int, Optional[str]]: ...
    
    def search_by_task(self, query: str, limit: int = 10, **kwargs) -> str: ...
    def get_task_status(self, task_id: str) -> Optional[Dict[str, Any]]: ...
    def cancel_search(self, task_id: str) -> bool: ...
    def get_stats(self) -> Dict[str, Any]: ...
```

#### 全局函数
```python
def get_task_registry() -> TaskRegistry: ...
def create_engine(dimension: int = None, max_tasks: int = None, config: GatewayConfig = None) -> SemanticSearchEngine: ...
```

### 23.2 缓存失效 (`sensory/cache_invalidation.py`)

#### 类: `CacheEntry`
```python
@dataclass
class CacheEntry:
    key: str
    value: Any
    created_at: float
    last_access: float
    access_count: int = 0
    tags: List[str] = field(default_factory=list)
```

#### 类: `HybridCacheInvalidator`
```python
class HybridCacheInvalidator:
    def invalidate(self, key: str) -> bool: ...
    def invalidate_pattern(self, pattern: str) -> int: ...
    def invalidate_by_tag(self, tag: str) -> int: ...
    def invalidate_all(self): ...
    def get_stats(self) -> Dict[str, Any]: ...
```

#### 类: `MemorySensoryIndex`
```python
class MemorySensoryIndex:
    def index(self, key: str, content: str, metadata: Dict = None): ...
    def search(self, query: str, limit: int = 10) -> List[Tuple[str, float]]: ...
    def invalidate(self, key: str): ...
    def clear(self): ...
```

---

## 24. task — 任务管理

### 24.1 (`task/task_manager.py`)

#### 枚举: `TaskStatus`
（同agent/models.py，参见1.1节）

#### 枚举: `PhaseType`
```python
class PhaseType(Enum):
    PLANNING = "planning"    # 规划阶段
    EXECUTING = "executing"  # 执行阶段
    VERIFYING = "verifying"  # 验证阶段
    REPORTING = "reporting" # 报告阶段
```

#### 枚举: `CircuitState`
（同health/enums.py，参见11.1节）

#### 类: `TaskContext`
```python
@dataclass
class TaskContext:
    task_id: str
    name: str
    description: str = ""
    created_at: float = field(default_factory=time.time)
    updated_at: float = field(default_factory=time.time)
    status: TaskStatus = TaskStatus.PENDING
    current_phase: PhaseType = PhaseType.PLANNING
    metadata: Dict[str, Any] = field(default_factory=dict)
    retry_count: int = 0
    max_retries: int = 3
    timeout: float = 300.0
```

#### 类: `CircuitBreaker`
```python
@dataclass
class CircuitBreaker:
    failure_count: int = 0
    success_count: int = 0
    state: CircuitState = CircuitState.CLOSED
    last_failure_time: float = 0.0
    last_success_time: float = 0.0
    failure_threshold: int = 5
    success_threshold: int = 2
    timeout: float = 30.0
    half_open_max_calls: int = 3
    half_open_calls: int = 0
```

#### 类: `TaskManager`
```python
class TaskManager:
    def __init__(self): ...
    
    # ---- 任务注册与管理 ----
    def register_task(self, name: str, description: str = "", max_retries: int = 3, timeout: float = 300.0, metadata: Optional[Dict[str, Any]] = None) -> str: ...
    def get_task(self, task_id: str) -> Optional[TaskContext]: ...
    def list_tasks(self, status: Optional[TaskStatus] = None) -> List[TaskContext]: ...
    def cancel_task(self, task_id: str) -> bool: ...
    def delete_task(self, task_id: str) -> bool: ...
    
    # ---- 状态机管理 ----
    def transition_to(self, task_id: str, new_status: TaskStatus) -> bool: ...
    
    # ---- 阶段协议 ----
    def set_phase(self, task_id: str, phase: PhaseType) -> bool: ...
    def register_phase_handler(self, phase: PhaseType, handler: Callable) -> None: ...
    async def execute_phase(self, task_id: str, phase: PhaseType, executor: Callable) -> Any: ...
    
    # ---- 熔断机制 ----
    def _record_success(self, task_id: str): ...
    def _record_failure(self, task_id: str): ...
    def get_circuit_state(self, task_id: str) -> CircuitState: ...
    def reset_circuit(self, task_id: str): ...
    def is_circuit_closed(self, task_id: str) -> bool: ...
    
    # ---- 观察者模式 ----
    def register_observer(self, observer: Callable) -> None: ...
    
    # ---- 任务执行流程 ----
    async def run_task(self, task_id: str, planning_fn: Optional[Callable] = None, executing_fn: Optional[Callable] = None, verifying_fn: Optional[Callable] = None, reporting_fn: Optional[Callable] = None) -> Dict[str, Any]: ...
    async def run_tasks_batch(self, task_ids: List[str], planning_fn: Optional[Callable] = None, executing_fn: Optional[Callable] = None) -> List[Dict[str, Any]]: ...
    
    # ---- 统计信息 ----
    def get_stats(self) -> Dict[str, Any]: ...
```

#### 异常
```python
class CircuitOpenError(Exception): ...
class TaskNotFoundError(Exception): ...
```

#### 全局函数
```python
def get_default_manager() -> TaskManager: ...
def create_task(name: str, description: str = "", **kwargs) -> str: ...
```

---

## 25. utils — 工具函数

### 25.1 (`utils/deepseek_client.py`)

#### 类: `LLMConfig`
```python
@dataclass
class LLMConfig:
    api_key: str = ""
    base_url: str = "https://api.deepseek.com"
    model: str = "deepseek-chat"
    max_tokens: int = 4096
    temperature: float = 0.7
    timeout: float = 30.0
```

#### 类: `RateLimiter`
```python
class RateLimiter:
    def __init__(self, max_requests_per_minute: int = 60): ...
    def acquire(self) -> bool: ...  # 返回是否获准
    def wait_and_acquire(self) -> None: ...  # 阻塞直到获准
```

#### 类: `DeepSeekClient`
```python
class DeepSeekClient:
    def __init__(self, config: Optional[LLMConfig] = None): ...
    
    def complete(self, prompt: str, max_tokens: int = None, temperature: float = None) -> str: ...
    async def complete_async(self, prompt: str, max_tokens: int = None, temperature: float = None) -> str: ...
    def embed(self, text: str) -> List[float]: ...
    def get_stats(self) -> Dict[str, Any]: ...
```

#### 全局函数
```python
def get_client() -> DeepSeekClient: ...
```

---

## 附录: 模块依赖图

```
agent ──────────────────────┐
  ├── models                 │
  ├── role_registry          │
  ├── lifecycle_manager ─────┤
  └── task_dispatcher ───────┤
                             │
audit ───────────────────────┤
                             │
base_wal ────────────────────┤
                             │
classifier ──────────────────┤
                             │
cli ─────────────────────────┤
  ├── router                 │
  └── tui                    │
                             │
config ──────────────────────┤
                             │
evolve ──────────────────────┤
  ├── self_evolution         │
  └── three_ring_architecture│
                             │
extractor ───────────────────┤
  ├── adaptive_compression    │
  └── extractor              │
                             │
fence ───────────────────────┤
                             │
gateway ─────────────────────┼── interfaces (IMemoryVault)
  ├── LRUCache               │
  └── Config                 │
                             │
health ──────────────────────┤
  ├── checker                │
  ├── circuit_breaker        │
  ├── data_classes           │
  ├── diagnostic             │
  ├── enums                  │
  ├── metrics                │
  ├── panel                  │
  └── threshold               │
                             │
integrate ───────────────────┤
  ├── MemoryPalaceIntegration │
  ├── config                 │
  └── stats                  │
                             │
interfaces ──────────────────┤
  ├── IMemoryVault           │
  ├── FileSystemAdapter      │
  └── verify_interface       │
                             │
introspection ───────────────┤
  ├── dependency_graph       │
  ├── module_map_generator   │
  ├── modules                │
  ├── problem_detector       │
  └── status_api             │
                             │
memory_layer ────────────────┤
  └── rl_access              │
                             │
mini_agent ──────────────────┤
  ├── compact                │
  ├── hooks                  │
  └── registry               │
                             │
normalizer ──────────────────┤
  └── deduplicator           │
                             │
optimize ────────────────────┤
  └── adaptive_compression   │
                             │
permission ──────────────────┤
  └── engine                 │
                             │
pipeline ────────────────────┤
  └── cross_reference        │
                             │
plugin ──────────────────────┤
  ├── plugin                 │
  └── test_plugins           │
                             │
repair ──────────────────────┤
  └── backup_manager         │
                             │
sensory ─────────────────────┤
  ├── cache_invalidation      │
  └── semantic_search         │
                             │
task ────────────────────────┤
  └── task_manager           │
                             │
utils ───────────────────────┘
  └── deepseek_client
```

---

## 附录: 关键枚举速查

| 枚举 | 值 |
|------|-----|
| **RoleType** | RESEARCHER, DEVELOPER, VALIDATOR, RECORDER, COORDINATOR |
| **AgentState** | CREATED, RUNNING, IDLE, COMPLETED, FAILED, STOPPED |
| **TaskStatus** | PENDING, RUNNING, COMPLETED, FAILED, CANCELLED |
| **SpaceType** | PRIVATE, LIBRARY, PUBLIC |
| **Permission** | NONE, READ, WRITE, ADMIN |
| **MemoryTier** | HOT, WARM, COLD |
| **AuditLevel** | DEBUG(10), INFO(20), WARNING(30), ERROR(40), CRITICAL(50) |
| **AuditCategory** | READ(1), WRITE(2), DELETE(3), PERMISSION(4), SECURITY(5), SYSTEM(6), QUERY(7), EXPORT(8), IMPORT(9), CONFIG(10) |
| **RiskLevel** | LOW(1), MEDIUM(2), HIGH(3), CRITICAL(4) |
| **PermissionLevel** | READONLY(1), WORKSPACE_WRITE(2), DANGER_FULL_ACCESS(3), PROMPT(4), ALLOW(5) |
| **RuleAction** | ALLOW(1), DENY(2), ASK(3) |
| **PhaseType** | PLANNING, EXECUTING, VERIFYING, REPORTING |
| **CircuitState** | CLOSED, OPEN, HALF_OPEN |
| **HealthStatus** | OK, WARNING, ALERT, CRITICAL |
| **MemoryType** | EPISODIC, SEMANTIC, PROCEDURAL, ABSTRACT |
| **CompressionLevel** | MINIMAL, BALANCED, AGGRESSIVE, ULTRA |
| **SimilarityMethod** | EXACT, SEMANTIC, STRUCTURAL, FUZZY |
| **VerificationStatus** | VERIFIED, UNVERIFIED, HIGH_CONFIDENCE, LOW_CONFIDENCE, FAILED |
| **ImportanceLevel** | CRITICAL(1.0), HIGH(0.9), MEDIUM(0.7), LOW(0.5), MINIMAL(0.3) |

---

*文档生成: M1.2 接口提取任务*
